# Class: SnapshotEvent

Defined in: `Core/Graph/Event.h/.cpp`  
Inherits: `MarketEvent`

## Role

Carries pure order-book snapshot (`SnapshotMessage`).

## Key API

- `get_message()`
- `get_snapshot_data()`
- `get_last_market_timestamp()`

## Usage

Typical live output for snapshot-based exchange streamers.

