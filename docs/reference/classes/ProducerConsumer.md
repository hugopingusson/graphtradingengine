# Class: ProducerConsumer

Defined in: `Core/Node/Base/Node.h/.cpp`  
Inherits: `Producer`, `Consumer`

## Role

Hybrid node interface for components that both ingest events and emit computed state.

## Notes

Currently a structural base class with minimal implementation, available for future composite nodes.

