# Class: Time

Defined in: `Helper/TimeHelper.h/.cpp`

## Role

Validated time-of-day object.

## Key API

- constructor `(hour, minute, second)` with range checks
- `unixtime(Resolution)` -> seconds/nanoseconds since midnight
- `to_string()` -> `HH:MM:SS`

