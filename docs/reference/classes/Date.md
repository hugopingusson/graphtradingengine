# Class: Date

Defined in: `Helper/TimeHelper.h/.cpp`

## Role

Simple validated calendar date object.

## Key API

- constructor `(year, month, day)` with range checks
- `unixtime(Resolution)` -> epoch at 00:00:00
- `to_string()` -> `yyyy-mm-dd`

